from distutils.core import setup

setup(name='log_helper',
      version='@PACKAGE_VERSION@',
      package_dir={ '': '${CMAKE_CURRENT_SOURCE_DIR}/python' },
      description="Python lib for libLogHelper",
      packages=['log_helper'])